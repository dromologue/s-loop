- 8. The S Loop (SSSS)
  - Plain surface (the fifteen): Harden the bet, then ship in four steps (P13); Ship to be proven wrong (P15).
  - Comprehensive explanation for engineers: what SSSS changes versus a conventional SDLC and versus linear spec-driven development, and how the SDD primitives distribute across the four steps.
    - 7. Agent operating contract
      - Invariant. The skill leaving Ship carries spec, harness, prompts, guardrails and policy shape together. Miss one and you shipped code, not capability, and the loop has not closed.
      - Ship. INPUT the spec and passing tasks. PRODUCE the release, the telemetry, the instrumented skill. GATE bet settled on real signal, verdict recorded. STOP if the release yields no readable verdict.
      - Specify. INPUT the skeleton and its checks. PRODUCE EARS criteria bound to checks, atomic tasks, guardrails and policy shape. GATE every criterion maps to a check. STOP if a criterion has no oracle.
      - Scaffold. INPUT the scoped bet. PRODUCE skeleton, interfaces, fitness functions and eval harness in CI. GATE green on the skeleton, red on a seeded violation. STOP if a check cannot fail.
      - Scope. INPUT the trigger. PRODUCE problem statement, boundary, success and kill conditions, constraints. GATE a failure is definable. STOP if nothing would count as failure.
      - Run one change per turn. The spec is the source of truth; never hand-edit the running system into divergence from it. Do not pass a failing gate. Every step ends in an artefact, not a conversation.
    - 6. Objections a good engineer will raise, answered
      - "The four S names are marketing." The names are a mnemonic. The content is the oracle-first ordering and the per-step weighting. Judge it on those; if they do not hold up, the alliteration will not save it.
      - "Spec-as-source is a fantasy; specs drift." That is the movement's real failure, and it is what the close answers. The code is regenerated from the spec, and the skill carries a live eval harness that turns drift into a failing check. If your spec cannot fail a check, it is documentation, not source.
      - "This is TDD." Related, and narrower. TDD is red-green at unit level inside implementation. Oracle-first here means architectural fitness functions and an evaluation harness stood up before the behavioural spec, spanning the whole change and the non-deterministic model output a unit test cannot judge.
      - "This is just Spec Kit or Kiro." No. Those run the primitives as gated phases. SSSS interleaves them inside each step in shifting proportions, puts the oracle before the behaviour, and closes with a governed skill the tools do not produce.
      - "This is waterfall with new labels." No. Waterfall is linear, treats code as the source of truth, and writes tests last. SSSS is a loop run per change, treats the spec as the source of truth, and writes the oracle first.
    - 5. The four steps in depth, in engineering terms
      - Ship. Produce the deployed change, the telemetry that settles the Scope bet, and the instrumented skill filed back to the platform. Gate: the bet is settled against real signal and the verdict is recorded as the next change. A release with no readable verdict is not a Ship, it is an escape.
      - Specify. Produce the acceptance criteria in EARS, each bound to a check; the atomic task list; and the guardrails and policy shape the change must honour. Gate: every criterion maps to a check that can pass or fail. A criterion with no oracle does not ship.
      - Scaffold. Produce a running skeleton with named interfaces, the fitness functions and eval harness wired into CI, and seeded failing cases. Gate: the harness is green on the empty skeleton and red on a deliberately violated contract. A harness that cannot go red is not an oracle.
      - Scope. Produce a one-paragraph problem statement, an explicit in-and-out boundary, success and kill conditions, and the constraints the build inherits: latency and cost per call, data residency, and the platform policies the change is bound by. Gate: the change has a defined failure. If no observable outcome would count as failure, it is not a bet and Scaffold does not start.
    - 4. How the SDD primitives distribute across the four steps
      - Weighting at a glance (H heavy, M medium, L light, dash negligible). Scope: principles H, specify L, design dash, decompose dash, implement dash, evaluate dash. Scaffold: design H, evaluate H, specify M, decompose L, implement dash. Specify: specify H, decompose H, design M, evaluate M, implement L. Ship: implement H, evaluate H, release H, specify reference, design L.
      - Ship is dominated by implement, evaluate and release. The model generates against the spec, the harness runs and gates, the change goes out, and you read the telemetry. Specify is now reference, not authorship; the verdict updates the spec and the loop closes. This is Spec Kit's implement plus the release, the observation and the feedback that the linear pipeline stops just short of.
      - Specify is dominated by specify and decompose. Now you write the full acceptance criteria, in EARS or an equivalent, and break them into atomic tasks, each bound to a check that can pass or fail. Design is refinement only; implementation is still light. This is Kiro's requirements-and-tasks, written against a skeleton that can already contradict you rather than on a blank page.
      - Scaffold is dominated by design and by evaluate. You commit interfaces, module boundaries and technology, and in the same move you stand up the fitness functions and the eval harness. Specify appears only as the interface contracts the skeleton exposes; decomposition is light; there is no behavioural implementation yet. This is where SSSS departs hardest from linear SDD, which leaves the test scaffolding implicit and late; here it is explicit and first.
      - Scope is dominated by principles and framing. You set the constraints the change must honour, state the outcome it bets on, and write the kill condition. There is a thin slice of specify here, the outcome and not the behaviour, and essentially no design, decomposition or implementation. In Spec Kit terms this is constitution plus the first paragraph of specify, and nothing downstream.
      - The primitives are the familiar ones: set principles (Spec Kit's constitution), specify (EARS acceptance criteria), design (architecture, interfaces, technology choices), decompose (atomic tasks bound to criteria), implement (generate against the spec), evaluate (run the checks), and release. Each of the four steps is a different mix of them. The weightings are the point of SSSS, not the four names.
    - 3. What SSSS changes, and why
      - The close, briefly. Ship does not end the line; it emits a governed, instrumented skill, the spec bound to its harness, prompts, guardrails and policy shape, and feeds the result back as the next change. The SDD movement's own admitted failure is that specifications drift and go ungoverned. A closed loop whose output is a skill carrying a live eval harness is the governance the tools have not yet supplied.
      - Departure two: the primitives run in every step, not as a pipeline. This is the one worth arguing about. Spec Kit runs constitution, specify, plan, tasks and implement as ordered phases with gates between them. SSSS runs all of those primitives inside each of the four steps, with the centre of gravity shifting as you go round the loop. You are always doing some framing, some specifying, some designing, some decomposing, some building and some checking; what changes step to step is the proportion. That is closer to how competent engineers actually work than a five-stage relay, and it is what makes SSSS a loop of four mixed steps rather than an assembly line.
      - Departure one: the oracle comes first. Linear SDD derives tests from the specification, after the specification exists. SSSS stands the executable checks up in Scaffold, before the behavioural spec is written: architectural fitness functions, the evaluation harness for any model-driven behaviour, and the golden and regression cases the change must not break. The reason is blunt. When a model generates the code, "it looks correct" is worth nothing, and a check that cannot fail is not a check. Building the oracle before the behaviour makes every later step machine-decidable the moment it lands, including the non-deterministic parts a conventional test suite has no answer for.
    - 2. The baseline: what spec-driven development already gives you
      - If your team runs Spec Kit or Kiro well, you already have most of the value on this page. SSSS is not a rival tool; it is an opinionated way of sequencing the same primitives, with two departures. The rest of this explains those two departures and little else, because everything else it shares with the field.
      - Take the field as it stands in 2026, because SSSS assumes it rather than reinventing it. GitHub Spec Kit runs constitution, specify, plan, tasks and implement as commands over a repo. AWS Kiro splits the same work across requirements.md, design.md and tasks.md, with acceptance criteria in EARS form: when a trigger occurs, under a stated condition, the system shall produce a response. Tessl pushes furthest, treating the spec as the maintained artefact and regenerating code from it. The literature names three rigour levels, spec-first, spec-anchored and spec-as-source, and settles on one honest conclusion: when a model writes most of the code, the specification is the highest-leverage thing a human still writes.
    - 1. What the S Loop is, for an engineer
      - The S Loop (SSSS) is a build loop: Scope, Scaffold, Specify, Ship, run once per change and closed so that shipping starts the next turn rather than ending the line. It sits in the spec-driven development family, where a versioned specification is the source of truth and the running code is a projection you regenerate, not the asset you hand-edit. It maps onto the wider Dromologue framework, but you do not need any of that to use it. What follows is deliberately concrete, and it assumes you have already sat through a spec-first pitch and want to know what is actually different.
  - Overview
    - Ship: release and take reality's verdict; the verdict returns to Concept as a new Event.
    - Specify: declare intent with the precision the build requires.
    - Scaffold: stand up the structure, interfaces and fitness functions before the detail.
    - Scope: bound the work.
    - The build process, Level 4 Construction. Scope, Scaffold, Specify, Ship.
